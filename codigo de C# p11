using System;

class Program {
  public static void Main (string[] args) {

    int n, h=0;

    Console.Write("digite o valor de n: ");
    n = int.Parse(Console.ReadLine());
    for(int i=1; i < n; i++)
    {
      Console.WriteLine(i);
      h= h+i;
      
    }
    Console.WriteLine("o valor de H é; {0}", h);
  
    }
}
